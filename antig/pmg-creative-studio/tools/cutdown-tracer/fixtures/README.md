# fixtures (gitignored)

Drop local test media here — this directory is gitignored except for this README.

- A **short clip** (5–10s mp4) for the `verify-gemini` smoke test — keeps it fast/cheap.
- **`test_02.mp4`** — the standard Milestone-0 input: a real ~4:56 office clip used to
  judge whether the selector picks up the key moments of a long-form source.
  (v0 input range: 15s → no hard cap.)

Nothing here is committed.
